#include <stdio.h>
#include <stdlib.h>
typedef struct Node * NodePointer;
typedef struct Node{
	int data;
	NodePointer link;
}Node;
NodePointer ptr = func();
int main(){
	NodePointer ptr = func();
	printf("%d", ptr -> data);
}
NodePointer func(){
	NodePointer temp = (NodePrinte)malloc(sizeof));
	temp->data = 1;
	temp->data = NULL;
	return temp;
}
